# Core dependencies
fastapi==0.109.0
uvicorn[standard]==0.27.0
python-multipart==0.0.6
aiosmtplib==4.0.0
jinja2==3.1.3

# Database
sqlalchemy[asyncio]==2.0.25
asyncpg==0.29.0
alembic==1.13.1

# Pydantic and settings
pydantic==2.5.3
pydantic-settings==2.1.0
pydantic[email]==2.5.3

# Authentication & Security
python-jose[cryptography]==3.3.0
passlib==1.7.4
bcrypt==4.0.1
python-multipart==0.0.6

# Rate limiting & Caching
slowapi==0.1.9
# redis==5.0.1  # Optional - only needed for production with multiple instances

# Logging
loguru==0.7.2

# System Monitoring
psutil==6.0.0

# CORS
python-dotenv==1.0.0

# Testing (optional)
pytest==7.4.4
pytest-asyncio==0.23.3
httpx==0.26.0
aiosqlite==0.22.1

# Code quality (optional)
black==23.12.1
flake8==7.0.0
mypy==1.8.0

# AI
google-generativeai==0.3.2
openai==1.12.0
PyPDF2==3.0.1

# Excel import
openpyxl==3.1.2

# Meetings — .ics calendar invite generation
icalendar==5.0.12
python-dateutil==2.8.2
# Bundles the full IANA tz database with the app itself so ZoneInfo lookups (host/
# attendee timezone validation, meeting reminder math) don't depend on whatever
# tzdata happens to be installed on the host OS — some minimal/Docker Linux images
# ship a trimmed system tzdata missing legacy alias names (e.g. "Asia/Calcutta",
# still reported by some browsers) that are otherwise valid IANA identifiers.
tzdata==2024.1
