import type { CompanyStatus, CompanySize } from '../../../models/crm/company';

export const COMPANY_STATUSES: CompanyStatus[] = ['prospect', 'customer', 'churned', 'partner'];
export const COMPANY_SIZES: CompanySize[] = ['startup', 'small', 'medium', 'enterprise'];
